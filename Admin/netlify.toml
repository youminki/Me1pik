[build]
  publish = "dist"
  command = "yarn install && yarn build"
  environment = { NODE_VERSION = "20" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Admin 전용 환경 변수나 설정이 있다면 여기에 추가